<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itpiggy.android_basics">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidBasics">
        <activity
            android:name=".page.processor.ProcessorActivity"
            android:exported="true" />
        <activity
            android:name=".page.binder.AidlActivity"
            android:exported="true" />
        <activity
            android:name=".page.binder.MessengerActivity"
            android:exported="true" />
        <activity
            android:name=".page.broadcastreceiver.LocalReceiverActivity"
            android:exported="true" />
        <activity
            android:name=".mvvm.view.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".page.mmkv.MmkvActivity"
            android:exported="true" />
        <activity
            android:name=".page.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.handler.HandlerActivity" />
        <activity android:name=".page.handler.HandlerThreadActivity" />
        <activity android:name=".page.sharedpreferences.SharedPreferencesActivity" />
        <activity android:name=".mvc.controller.LoginActivity" />
        <activity android:name=".mvp.view.LoginActivity" />

        <service
            android:name=".page.binder.BookService"
            android:process=":remote" />
        <service
            android:name=".page.binder.BookMessengerService"
            android:process=":remote" />
    </application>

</manifest>